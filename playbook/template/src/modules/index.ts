import type { ModuleConfig } from "@/types/modules";

// === Register modules below this line ===
// Import and add your module configs to the array.
// Example:
//   import crm from "./crm";
//   export const modules: ModuleConfig[] = [crm];

export const modules: ModuleConfig[] = [];
