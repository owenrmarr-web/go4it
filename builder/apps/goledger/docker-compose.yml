services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=file:./dev.db
      - AUTH_SECRET=change-me-in-production
    volumes:
      - app-data:/app/prisma
volumes:
  app-data:
